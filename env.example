# Render Deployment Configuration
# This file contains the deployment settings for Render.com

# Build and deployment settings
NODE_ENV=production
PORT=10000

# Database settings (will be provided by Render PostgreSQL service)
# DATABASE_URL will be automatically set by Render

# JWT Configuration
# JWT_SECRET will be generated by Render

# Email Configuration (IMAP/SMTP)
# Set these in Render dashboard environment variables
EMAIL_USERNAME=your-email@domain.com
EMAIL_PASSWORD=your-app-password

# Cloudinary Configuration (for file uploads)
# Set these in Render dashboard environment variables
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# Stripe Configuration (for payments)
# Set these in Render dashboard environment variables
STRIPE_SECRET_KEY=sk_test_your-stripe-secret-key
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret

# Auth0 Configuration (if using Auth0)
# Set these in Render dashboard environment variables
AUTH0_DOMAIN=your-domain.auth0.com
AUTH0_CLIENT_ID=your-client-id
AUTH0_CLIENT_SECRET=your-client-secret
